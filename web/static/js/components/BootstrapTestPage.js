import React from 'react';
import { Input } from 'react-bootstrap';

const BootstrapTestPage = React.createClass({

  render() {
    return (
      <Input type="text" label="Default" />
    );
  },
});

export default BootstrapTestPage;
